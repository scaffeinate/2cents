<%= form_tag post_comments_path(post_id: post.id), remote: true do %>
  <%= text_area_tag 'content', nil, id: 'new-content', class: 'form-control', placeholder: 'Enter comment' %>
  <%= submit_tag 'Comment', id: 'comment', class: 'btn btn-primary' %>
<% end %>
