$('#feed').append("<%= j render @downvotes %>");

<% if @downvotes.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@downvotes) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $("#loader").remove();
  $(".pagination").remove();
<% end %>