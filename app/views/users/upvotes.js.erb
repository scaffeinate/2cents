$('#feed').append("<%= j render @upvotes %>");

<% if @upvotes.next_page %>
  $(".pagination").replaceWith("<%= j will_paginate(@upvotes) %>");
  $(".pagination").hide();
<% else %>
  $(window).off('scroll');
  $("#loader").remove();
  $(".pagination").remove();
<% end %>